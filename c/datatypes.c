#include <stdio.h>

main()
{
    int age = 40; // integer - целые числа в c
    double gpa = 3.5; // числа с точкой (дробные)
    char grade = 'A'; // буква/знак
    char phrase[] = "FooBar"; // массив из букв (строка)
    printf("Age %d, gpa %f, grade %c, phrase %s", age, gpa, grade, phrase); // d - int, f - float, c - char, s - char array
}
